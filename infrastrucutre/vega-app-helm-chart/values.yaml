# Default values for vega-app-helm-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount:
name:
namespace:

postgres_config:
  port:
  host:

secrets_config:
  db:
  user:
  password:


service:
  type:
  port:
  protocol:
  name:

ingress:
  class:
  scheme:
  type:
  ssl_redirect:
  backend_protocol:
  host:
  path:
  pathType:

image:
  repository:
  tag:
  pullPolicy:
  port:


